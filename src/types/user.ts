export interface User {
  id: number
  fullName: string
  email: string
  password: string
  // isActive: boolean
  // role: "admin" | "customer" | "seller"
  // address: string
  // createdAt: Date

  // Opcional fields
  // tags?: string[]
  // description?: string
}